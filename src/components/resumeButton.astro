---
import { siteConfig } from "../config";
---

{siteConfig.resume && (
  <a
    href={siteConfig.resume.link}
    target="_blank"
    rel="noopener noreferrer"
    class="inline-flex items-center rounded-xl bg-[var(--accent-color)] px-5 py-2 text-white font-semibold shadow-md transition hover:opacity-90"
  >
    {siteConfig.resume.label}
  </a>
)}
